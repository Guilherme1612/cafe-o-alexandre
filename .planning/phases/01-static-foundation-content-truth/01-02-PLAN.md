---
phase: 01-static-foundation-content-truth
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/pages/index.astro
  - src/styles/global.css
  - tests/static-build.test.mjs
autonomous: true
requirements:
  - VIS-05
  - ABOUT-03
  - UX-03
user_setup: []
must_haves:
  truths:
    - "The built page has one semantic document shell, one Café o Alexandre h1, readable empty-state copy, and every unknown business value visible as plain non-actionable text."
    - "The page renders the cafe object rather than duplicating business values in the Astro template."
    - "The visual foundation uses burgundy, charcoal, warm gold, and cream tokens with readable system typography, comfortable spacing, visible focus, and reduced-motion behavior."
    - "A static-build assertion catches sponsor branding, invented claims, or accidental actionable links in the generated page."
  artifacts:
    - "src/pages/index.astro imports the single `cafe` object and renders skip-link, header, main, named sections, and footer landmarks."
    - "src/styles/global.css defines the approved color, spacing, typography, focus, responsive, and reduced-motion tokens without gradients or motion effects."
    - "tests/static-build.test.mjs verifies `dist/index.html` after `astro build`."
  key_links:
    - "index.astro imports `cafe` from `src/data/cafe.ts` and emits every placeholder from that object."
    - "index.astro imports `src/styles/global.css`, so the token/focus contract is present in the generated static page."
    - "The build test inspects the same `dist/index.html` that a static host would serve."
---

<objective>
Render the stable semantic Astro shell and warm visual foundation from the shared content object, then prove the generated HTML preserves content truth.

Purpose: VIS-05 and ABOUT-03 need a useful no-JavaScript page that never turns missing facts into fake actions, while UX-03 needs the approved burgundy/charcoal/gold/cream system ready for later sections.
Output: `index.astro`, one global stylesheet, and a static-output assertion covering landmarks, placeholders, palette hooks, and sponsor/invention guardrails.
</objective>

<prohibitions>
- Do not put an address, phone, hours, social handle, offering, map destination, image filename, sponsor name, sponsor logo, or sponsor phone directly in the Astro template or CSS.
- Do not emit an `href`, `tel:`, WhatsApp URL, Maps URL, click handler, fake disabled button, or icon-only action for any unresolved placeholder.
- Do not add contact-action, map, gallery, third-party font, animation, gradient, or hydrated-island behavior in this foundation shell.
</prohibitions>

## Artifacts this phase produces

- A single static page with semantic landmarks and a visible placeholder state.
- Global CSS tokens and responsive rules that later sections can inherit unchanged.
- A build-output check that protects the public artifact, not just source text.

<execution_context>
@/Users/guilherme/.codex/gsd-core/workflows/execute-plan.md
@/Users/guilherme/.codex/gsd-core/templates/summary.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-static-foundation-content-truth/01-RESEARCH.md
@.planning/phases/01-static-foundation-content-truth/01-UI-SPEC.md
@.planning/phases/01-static-foundation-content-truth/01-PATTERNS.md
@.planning/phases/01-static-foundation-content-truth/01-01-PLAN.md
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: Render the semantic placeholder shell and visual tokens</name>
  <files>src/pages/index.astro, src/styles/global.css</files>
  <read_first>src/data/cafe.ts; .planning/phases/01-static-foundation-content-truth/01-UI-SPEC.md; .planning/phases/01-static-foundation-content-truth/01-RESEARCH.md; .planning/PROJECT.md</read_first>
  <action>Build `src/pages/index.astro` as the only page entry: import `cafe` and the global stylesheet; emit a document title from `cafe.name`; include exactly `&lt;meta name="viewport" content="width=device-width, initial-scale=1"&gt;`; add a first-focusable `Skip to content` link, a dark charcoal hero/header band, a small warm-gold eyebrow, a cream display title, a `header`, `main#main-content`, named `section` landmarks, and a `footer`; render exactly one h1 with `cafe.name`; render `cafe.welcome` as the short welcome sentence and `cafe.phoneFallback` as the non-actionable next-step label `Phone number not yet available`; render the approved empty-state heading/body and every address/contact/hours/social/map/description/images value as a labeled, plain-text placeholder from `cafe` (never as a link). Keep section headings as ordered h2 elements with no skipped levels. Keep labels structural and avoid any unverified café claim. In `src/styles/global.css`, define the UI contract tokens (cream `#F7F0E4`, charcoal `#272323`, burgundy `#762536`, warm gold `#C39A5A`, 4px-based spacing, system sans/serif type at 14/16/24/40px, max-width 72rem, 8px radii), mobile-first 16px gutters with 768px/1200px adjustments, 48px section rhythm, dashed placeholder treatment, 3px gold `:focus-visible` outline with 2px offset, and the 120ms reduced-motion-safe transition. Keep text within 65ch, preserve a 320px/200%-zoom layout with fluid widths, wrapping, and no fixed-height content, and use no gradients, shadows beyond the approved subtle elevation, or entrance/scroll/autoplay effects.</action>
  <acceptance_criteria>The page includes the exact viewport meta, dark hero band, gold eyebrow, cream display title, source-backed welcome sentence, and plain-text `Phone number not yet available` next-step; it builds with one h1, ordered h2 headings, and complete semantic landmarks; all unresolved values are visibly bracketed informational text with no actionable destination; CSS exposes the four approved colors, readable contrast-oriented type, responsive gutters, touch-sized controls, visible focus, and reduced-motion behavior.</acceptance_criteria>
  <verification>Automated: run the Astro static build after rendering the page and stylesheet.</verification>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>`src/pages/index.astro` renders the shared `cafe` data in an accessible static shell and `src/styles/global.css` supplies the approved warm visual system without inventing or activating café facts.</done>
</task>

<task type="auto">
  <name>Task 2: Assert the generated static artifact</name>
  <files>tests/static-build.test.mjs</files>
  <read_first>src/pages/index.astro; src/styles/global.css; src/data/cafe.ts; .planning/REQUIREMENTS.md; .planning/phases/01-static-foundation-content-truth/01-UI-SPEC.md</read_first>
  <action>Create `tests/static-build.test.mjs` with Node's built-in test/assert APIs. Read `dist/index.html` produced by `astro build` and assert the exact viewport meta, approved café title, dark hero/gold-eyebrow/cream-display hooks, source-backed welcome sentence, non-actionable `Phone number not yet available` label, exactly one h1, ordered h2 headings, skip-link/main/header/section/footer landmarks, all bracketed placeholders, and the empty-state copy are present. Assert the built HTML has no `tel:`, WhatsApp, Maps, social, image, sponsor, or other external/actionable destination generated from unresolved data, and no sponsor marker text. Also read `src/styles/global.css` in the test to assert the four required color tokens, 4px spacing scale, 14/16/24/40px typography, 72rem/65ch width limits, 768px and 1200px responsive rules, fluid 320px/200%-zoom-safe sizing without fixed-height content, `:focus-visible` styling, and the reduced-motion media rule remain declared. Keep checks deterministic and file-local.</action>
  <acceptance_criteria>The test fails if the static artifact drops the viewport contract, hierarchy/copy, placeholder, h1/h2/landmark, or non-actionable-action guard; introduces sponsor markers; or loses the palette, typography, spacing, responsive/zoom safety, focus visibility, or reduced-motion declarations.</acceptance_criteria>
  <verification>Automated: build the static artifact and run the complete Node test set.</verification>
  <verify>
    <automated>npm run build && npm test</automated>
  </verify>
  <done>A single command proves the production-like `dist/index.html` preserves content truth, semantic structure, and visual-token declarations.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `src/data/cafe.ts` → Astro template | Static values become public HTML through interpolation. |
| Generated HTML/CSS → visitor browser | The browser consumes the built artifact without a runtime application layer. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-04 | Tampering | `src/pages/index.astro` | high | mitigate | Keep business values sourced through `cafe`, use Astro's escaped interpolation, and run the generated-artifact test for placeholders and prohibited destinations. |
| T-01-05 | Information disclosure | Static HTML/CSS | medium | mitigate | Render only approved identity, structural empty-state text, and explicit placeholders; do not include sponsor or private source material. |
| T-01-06 | Denial of service | Layout/CSS | low | mitigate | Avoid external fonts/scripts, fixed-height content, heavy effects, and third-party embeds so the page remains usable with JavaScript disabled or dependencies unavailable. |
</threat_model>

<verification>
Run `npm run build && node --test tests/static-build.test.mjs`; inspect `dist/index.html` for the viewport meta, dark hero hierarchy, welcome/next-step copy, one h1, ordered h2 headings, named landmarks, all visible bracketed placeholders, no actionable unknown URLs or sponsor markers, and CSS declarations for typography/spacing, 320px/200% viewport safety, focus visibility, palette, and reduced motion.
</verification>

## Source coverage audit

| Source | Items | Plan coverage |
|--------|-------|---------------|
| GOAL | Minimal static Astro page, one explicit content source, semantic shell, warm tokens, no invented facts | 01-01 establishes the static/content boundary; 01-02 renders and verifies the shell/tokens. |
| REQ | VIS-05, ABOUT-03, UX-03 | 01-01 guards placeholders and source truth; 01-02 verifies rendered placeholders and the visual system. |
| RESEARCH | Astro static output, Node 22+, one data module, semantic HTML, CSS tokens, visible placeholders, no runtime/adapter/dependency sprawl | Tasks 01-01.1, 01-01.2, and 01-02.1 implement; 01-02.2 checks the built artifact. |
| CONTEXT | STATE decisions require static-first Astro, a single shared object, visibly non-actionable unknowns, and no invented/sponsor-derived facts | Prohibitions and source/build assertions in both plans enforce each decision; no phase context file adds further scope. |

<success_criteria>
- The static page renders from `cafe` with no duplicated or invented café facts.
- Unknown values remain readable plain text and do not receive links, handlers, or fake controls.
- The built output carries the warm visual tokens, responsive/focus/reduced-motion rules, and semantic shell expected by later phases.
</success_criteria>

<output>
Create `.planning/phases/01-static-foundation-content-truth/01-02-SUMMARY.md` when done.
</output>
