---
phase: 03-caf-photo-gallery
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data/cafe.ts
  - src/pages/index.astro
  - src/styles/global.css
  - tests/content-truth.test.mjs
  - tests/static-build.test.mjs
autonomous: true
requirements:
  - GALLERY-01
  - GALLERY-02
  - GALLERY-03
user_setup: []
must_haves:
  truths:
    - "The Café images section is a truthful gallery surface: it renders supplied local café figures when present and a clear Photos coming soon state while no café images have been supplied."
    - "Future gallery images have meaningful alt text, visible captions, lazy loading, and responsive dimensions without JavaScript or a lightbox."
    - "The sponsor advertisement is never used as a gallery asset, and unresolved image placeholders never become src/href destinations."
  artifacts:
    - "src/data/cafe.ts defines a typed gallery metadata array that is empty until verified café images arrive."
    - "src/pages/index.astro renders the gallery empty state or semantic figure cards from cafe.gallery."
    - "src/styles/global.css defines the one/two/three-column responsive gallery grid using existing tokens."
    - "tests/content-truth.test.mjs and tests/static-build.test.mjs guard image placeholder and alt/caption truth."
  key_links:
    - "Gallery figure src, alt, caption, width, and height are all sourced from cafe.gallery."
    - "The static output test verifies no image element is emitted while gallery data is empty."
---

<objective>
Add a small, accessible static gallery structure without inventing photos that were not supplied.

Purpose: satisfy the gallery contract honestly today and make the eventual local café image drop a data-only update.
Output: typed empty gallery data, conditional semantic markup, responsive CSS, and static regression checks.
</objective>

<prohibitions>
- Do not use the supplied sponsor advertisement as a gallery image or copy its branding/layout.
- Do not invent café photos, filenames, alt text, captions, or visual claims.
- Do not add PhotoSwipe, a lightbox, modal, remote image host, image CDN, framework island, or client-side gallery runtime.
- Do not render broken `<img>` elements or placeholder strings as `src`/`href` values.
</prohibitions>

<tasks>

<task type="auto">
  <name>Task 1: Add typed gallery metadata and safe rendering</name>
  <files>src/data/cafe.ts; src/pages/index.astro</files>
  <read_first>src/data/cafe.ts; src/pages/index.astro; .planning/phases/03-caf-photo-gallery/03-RESEARCH.md; .planning/phases/03-caf-photo-gallery/03-UI-SPEC.md; .planning/REQUIREMENTS.md</read_first>
  <action>Add a readonly gallery image type with local src, meaningful alt, visible caption, width, and height; initialize `cafe.gallery` to an empty array because no café images are present. Replace the current image placeholder paragraph with a conditional gallery: render a bordered “Photos coming soon” empty state when the array is empty, otherwise render semantic figure/img/figcaption cards with local metadata, lazy loading, and async decoding. Keep the existing `cafe.images` placeholder available as content truth and never use it as a src.</action>
  <acceptance_criteria>The current build contains a clear Photos coming soon state and no img/src for unresolved data; future array entries have all required metadata and are rendered with visible captions and descriptive alt text.</acceptance_criteria>
  <verification>Run `npm run build` and inspect `dist/index.html` for the empty state and absence of image destinations.</verification>
  <verify><automated>npm run build</automated></verify>
  <done>The page has a truthful gallery surface ready for supplied local café assets.</done>
</task>

<task type="auto">
  <name>Task 2: Style and guard the responsive gallery</name>
  <files>src/styles/global.css; tests/content-truth.test.mjs; tests/static-build.test.mjs</files>
  <read_first>src/styles/global.css; tests/content-truth.test.mjs; tests/static-build.test.mjs; .planning/phases/03-caf-photo-gallery/03-PATTERNS.md</read_first>
  <action>Add token-based gallery grid/card/empty-state styles with one column by default, two columns at 768px, and three at 1200px; use aspect-ratio/object-fit without fixed page heights, preserve readable captions, 320px/200% wrapping, and reduced-motion behavior. Extend Node tests to require the gallery field/placeholder and assert the generated empty state, no unresolved image src/href, semantic image attributes when entries exist, and sponsor-marker absence.</action>
  <acceptance_criteria>Build and tests fail if the empty state disappears, a placeholder becomes an image destination, image metadata loses alt/caption/lazy loading, the responsive grid contract is removed, or sponsor branding enters the artifact.</acceptance_criteria>
  <verification>Run the full build/test suite and perform a browser smoke at 320px, 768px, and 1200px with reduced motion.</verification>
  <verify><automated>npm run build && npm test</automated></verify>
  <done>The gallery contract is statically verified and visually safe at the supported widths.</done>
</task>

</tasks>

<verification>
Run `npm run build && npm test`; use the local browser to confirm no horizontal overflow and readable “Photos coming soon” fallback at 320px, 768px, and 1200px. Add real image entries only when the user supplies café photos and captions.
</verification>
