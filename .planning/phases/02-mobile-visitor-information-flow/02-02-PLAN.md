---
phase: 02-mobile-visitor-information-flow
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - tests/content-truth.test.mjs
  - tests/static-build.test.mjs
autonomous: true
requirements:
  - VIS-01
  - VIS-02
  - VIS-03
  - VIS-04
  - ABOUT-01
  - ABOUT-02
  - MAP-01
  - MAP-02
  - UX-01
  - UX-02
  - UX-04
user_setup: []
must_haves:
  truths:
    - "Tests protect the visitor information contract in the production-like static artifact, not only the source module."
    - "Tests fail if unresolved placeholders become actionable links or if required mobile/accessibility landmarks disappear."
  artifacts:
    - "tests/content-truth.test.mjs asserts the complete Phase 2 data shape and placeholder safety."
    - "tests/static-build.test.mjs asserts hero actions, details, About Us, directions fallback, footer repetition, and responsive/accessibility hooks."
  key_links:
    - "The tests read dist/index.html after npm run build, matching what static hosting serves."
    - "Placeholder assertions cover each action-bearing field and prevent accidental sponsor/invented output."
---

<objective>
Expand the small Node test suite so future content updates cannot regress mobile visitor actions, content truth, or accessible static output.

Purpose: make Phase 2 verifiable without adding a testing framework or runtime dependency.
Output: deterministic source and generated-artifact assertions covering all eleven requirements.
</objective>

<prohibitions>
- Do not introduce a test framework, browser dependency, network call, map iframe, or visual snapshot tool.
- Do not loosen placeholder assertions to make a failing implementation pass.
</prohibitions>

<tasks>

<task type="auto">
  <name>Task 1: Assert the expanded content object</name>
  <files>tests/content-truth.test.mjs</files>
  <read_first>tests/content-truth.test.mjs; src/data/cafe.ts; .planning/REQUIREMENTS.md</read_first>
  <action>Extend the built-in Node tests to require address, phone, WhatsApp, opening hours, social links, map link, description, and offerings fields. Assert each unresolved placeholder remains explicit and that placeholder values are not silently replaced by sponsor or invented content.</action>
  <acceptance_criteria>The source test catches missing fields, changed placeholder labels, and sponsor/invented values while allowing future verified values to be added deliberately.</acceptance_criteria>
  <verification>Run the focused Node test.</verification>
  <verify><automated>npm test -- tests/content-truth.test.mjs</automated></verify>
  <done>The data contract has a small regression guard.</done>
</task>

<task type="auto">
  <name>Task 2: Assert visitor flow in generated HTML and CSS</name>
  <files>tests/static-build.test.mjs</files>
  <read_first>tests/static-build.test.mjs; src/pages/index.astro; src/styles/global.css; .planning/phases/02-mobile-visitor-information-flow/02-UI-SPEC.md</read_first>
  <action>Add assertions for hero address and action labels, practical-information labels, About Us and offer empty states, directions fallback, footer repetition, and native href gating. Assert one h1, ordered headings, named landmarks, placeholder visibility, no unresolved tel:/WhatsApp/Maps/social destinations, accessible focus/touch hooks, responsive breakpoints, fluid sizing, and reduced-motion declarations.</action>
  <acceptance_criteria>The generated-artifact test fails on missing visitor flow content, fake actionable placeholders, sponsor markers, heading/landmark regressions, or lost responsive/accessibility safeguards.</acceptance_criteria>
  <verification>Build the page and run the complete test suite.</verification>
  <verify><automated>npm run build && npm test</automated></verify>
  <done>Phase 2 has a deterministic check for the public static page and its CSS contract.</done>
</task>

</tasks>

<verification>
Run `npm run build && npm test`; all content and static-output tests pass with unresolved café values still represented as non-actionable placeholders.
</verification>
